{
  "name": "nandp-website",
  "version": "1.0.0",
  "scripts": {
    "start": "parcel src/templates/index.html",
    "build": "parcel build src/templates/index.html --dist-dir dist",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "gh-pages": "^6.1.1",
    "parcel": "^2.12.0",
    "tailwindcss": "^3.4.4"
  },
  "dependencies": {
    "swiper": "^11.1.4"
  }
}
